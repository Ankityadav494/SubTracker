# Render Blueprint — https://render.com/docs/blueprint-spec
services:
  - type: web
    name: subtracker-api
    runtime: node
    rootDir: apps/backend
    buildCommand: npm ci
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "20"
